#/bin/sh
cells --component:lint
R=$?
if [ R != 0 ]
        then
	echo $R
        exit $R
fi

cells --component:coverage
R=$?
if [ R != 0 ]
        then
	echo $R
        exit $R
fi

cells --component:test-demo
R=$?
if [ R != 0 ]
        then
	echo $R
        exit $R
fi
